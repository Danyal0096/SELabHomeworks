# Java / Maven / Gradle output (scoped to exp3)
target/
build/
.gradle/
out/
*.class
*.jar
*.war
*.ear

# IDE / OS generated files
.idea/
*.iml
.project
.classpath
.settings/
.vscode/
.DS_Store
Thumbs.db

# Reports generated by build plugins live in target/ or build/ by default.
# Preserve selected real outputs by copying them into evidence/ with clear provenance.
# Do NOT ignore evidence/*.txt, screenshots, or manually prepared documentation.
